import { describe, it, expect } from "vitest";

/**
 * Phase 1 smoke test — confirms the Vitest scaffold is working.
 * Real unit tests are added in Phase 6.
 */
describe("Phase 1 scaffold", () => {
  it("vitest is configured and running", () => {
    expect(1 + 1).toBe(2);
  });
});
